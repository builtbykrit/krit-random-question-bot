const fs = require("fs");

const questions = require("./questions.js");
console.log({ questions });
// questions = questions.filter((q) => !q.hasBeenAsked);
// const question = questions[Math.floor(Math.random() * questions.length)];
// question.hasBeenAsked = true;

// questions.find((q) => q.question === question.question);
// fs.writeFile("questions.js", JSON.stringify(questions));
